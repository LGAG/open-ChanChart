# 缠论中枢识别算法

> 本文档定义 ChanChart 的中枢识别算法。每个规则标注**证据来源**:
> - 【原文】= 缠师《教你炒股票》原文明确(附课程编号)
> - 【工程取舍】= 本项目/开源项目的实现选择,非缠师原文
>
> 严格区分二者,避免把工程取舍当成缠师本意。

---

## 0. 研究结论(决策依据)

2026-07 基于缠师原文 + 主流开源实现(Vespa314/chan.py、tomcat123a/-chanlun)研究,得出以下关键结论:

### 0.1 中枢的原文定义【原文】

- **第17课《走势终完美》**:「某级别走势类型中,被至少三个连续**次级别走势类型**所重叠的部分,称为缠中说禅走势中枢。」
- **第18课**:给出精确计算——「次级别的连续三个走势类型 A、B、C,分别的高、低点是 a1\a2, b1\b2, c1\c2。则,中枢的区间就是 `(max(a2,b2,c2), min(a1,b1,c1))`」
- **关键**:原文说"次级别走势类型",**既未说"三笔"也未说"三段"**。

### 0.2 "次级别走势类型"≠ 笔 ≠ 段【原文】

- 第17课原理二:「任何级别任何完成的走势类型,必然包含一个以上的缠中说禅走势中枢」——走势类型是**含中枢的完整走势**。
- 第62课:笔、线段是独立于中枢定义的图形构件(笔=相邻顶底分型之间;线段=至少三笔)。
- 第101课递归链:分型→笔→线段→不同级别走势类型(含中枢)。
- **结论**:笔/线段是走势类型之下的底层构件,"次级别走势类型"是比线段更高一层的概念。原文中枢构件既不是笔也不是线段本身。

### 0.3 这是递归定义,有循环依赖【原文 + 工程取舍】

- 第17课承认递归基底问题:最低不可分解级别,中枢"定义为至少三个该级别单位 K 线重叠部分"。
- 第63课实操:选定最低级别图→有线段→定义该级别中枢→递归上去。
- **后人(龙哥量化等)为可操作性,把"次级别走势类型"实操化为"线段"**,理由:"只有线段才是不需要依靠走势中枢即可确立的已知概念",避免循环定义。**这是工程取舍,非原文。**

### 0.4 "笔中枢 / 段中枢"都不是原文术语【工程取舍】

- 缠师原文(第17/18/62/63/67/77/101课)未出现"笔中枢""段中枢""线段中枢"这些词。
- "笔中枢"仅见于后人整理者批注(「很多人说缠论早期的中枢是笔中枢」)。
- **结论**:用笔还是段构造中枢,是工程选择,原文不约束。

### 0.5 "不跨段"是后人演绎,非原文【工程取舍】

- 缠师原文**未规定**"中枢必须被线段边界约束(不跨段)"。
- 递归方向是"线段→中枢→走势类型→高级别中枢",中枢由构件向上构建,而非被线段边界向下约束。
- **结论**:"不跨段"是工程取舍(Vespa314 默认即此),不是缠师要求。将来若发现不跨段导致中枢偏少/位置怪,可放开成跨段,非理论错误。

### 0.6 四价位 ZG/ZD/GG/DD【原文间接】

- ZG = min(高点),ZD = max(低点) —— 中枢区间上下沿
- GG = max(高点),DD = min(低点) —— 中枢构成三段(笔)的最高/最低
- min/max 对应关系是标准释义,**字母符号体系是否原文引入无法核实**(可能后人标注)。

### 0.7 9段上限、破坏判定【后人演绎 / 原文间接】

- "延伸最多9段、9段升级"——**后人演绎**,原文只有级别递归框架,无9段数字阈值。
- 中枢破坏——社区归于第36课《走势中枢的破坏》,判定"离开后回抽不回中枢区间",与第三类买卖点相关。原文措辞无法联网核实,标间接。

### 0.8 区间笔数 / 悬空进入离开笔【工程取舍】

- 第18课公式 `(max(a2,b2,c2), min(a1,b1,c1))` 描述 3 个走势 A/B/C 重叠,**未明确**这 3 走势是否包含进入/离开走势(原文措辞无法联网核实)。
- Vespa314 `normal`(默认)只取 **2 根反向笔**定 ZG/ZD,进入笔 `bi_in`/离开笔 `bi_out` 悬空外不算;`over_seg` 取 3 笔。
- tomcat123a 同样把 entering seg 排除在 pivot 外。
- 「悬空的两笔」(进入笔/离开笔)是缠论图示的结构,但**本项目工程上不单独建模**——成中枢 3 笔 p1,p2,p3 全参与 ZG/ZD(近 `over_seg`)。这是工程取舍,非原文硬性要求。

### 0.9 尾部 K 线丢弃【已处理·虚拟分型方案】

- 笔算法平均丢弃尾部 ~10 根 K 线(诊断:0/183 次试验最新 K 线在任何笔内),根因是「分型需 3 根 K 线确认」,最后一根无"右侧"无法确认分型。
- **2026-07-19 最终方案:虚拟分型**。构造一个虚拟分型塞进 `fractals`,让 `generate_new_pens` 自行决定延伸/新增虚拟笔(而非外部画一根虚拟笔)。
  - 虚拟分型中心 = 最新缠论K线 `klines[n-1]`,左 = `klines[n-2]`,"右"是假设的未来K线。
  - 方向由尾部走势决定:最后分型 top 且 `center.high > last.high` → 虚拟 top(同向延伸);否则 → 虚拟 bottom(反向虚拟笔)。bottom 对称。
  - 透传 `is_sure`:虚拟分型 `is_sure=False`;笔的 `is_sure = f1.is_sure and f2.is_sure`(任一端虚拟则整笔虚拟);`validate_pens` 合并平行笔时 `prev.is_sure &= curr.is_sure`。
  - **参与段/中枢计算**(用户决定):虚拟笔流入段/中枢,右端结构随新K线变动。
  - 前端按 `is_sure` 拆实线/虚线(虚线 opacity 0.6)。
- 缠论原文无尾部正式规则(定义是完成态)。
- **2026-07-19 迭代史**:先实现"仅显示延伸版虚拟笔"(不参与计算),用户认为不符合要求已回退;改为"虚拟分型塞进笔算法 + 参与计算"方案(本节)。

---

## 1. 本项目的算法选择

基于 0.4、0.5,本项目作出如下工程取舍:

| 决策 | 选择 | 依据 |
|---|---|---|
| 构造基元 | **笔** | 0.4 笔/段皆可;本项目笔算法成熟,Vespa314 默认亦用笔 |
| 跨段与否 | **不跨段**(段内笔中枢) | 0.5 非原文但工程合理;Vespa314 `zs_algo=normal` 同此 |
| 成中枢公式 | `min(高点) > max(低点)` | 0.1 第18课原文公式;三个开源实现共识 |
| **区间笔数** | **3 笔都算 ZG/ZD**(p1,p2,p3 全参与) | 见下方「区间笔数模型」说明 |
| 9段上限 | **不采纳** | 0.7 后人演绎,非原文;MVP 不做级别升级 |
| 破坏判定 | 离开后回抽不回区间才破坏(两笔确认) | 0.7 原文(第20课中枢重新定理);笔高低点用全程极值(§3.6)否则退化为简化版 |

**区间笔数模型(2026-07-19 核对 Vespa314 源码后修正)**:

本项目成中枢用连续 3 笔 p1,p2,p3(反向笔起,p2 为中间同向笔),**3 笔全程高低点都参与** ZG/ZD 计算。这等同 Vespa314 `zs_algo=over_seg` 模式(`lst[-3:]`,3 笔区间),**不是** `normal` 模式。

- Vespa314 `normal`(默认):`lst = lst[-2:]`,区域由 **2 根反向笔**界定,中间同向笔 p2 **不算** ZG/ZD;进入笔 `bi_in`/离开笔 `bi_out` 悬空在外不参与区间。
- Vespa314 `over_seg`:`lst[-3:]`,3 笔都算。
- 本项目:3 笔都算,故区间偏窄(被中间回抽笔 p2 卡住),比 `normal` 保守。
- **实测影响**:3 笔 vs 2 反向笔,15.2% 的中枢 ZG/ZD 不同(348279 个样本)。
- 「悬空的两笔」(进入笔/离开笔)在缠论图示中存在,但本项目工程上选择不单独建模——把进入笔 p1 也算进区间。这是工程取舍,非原文明确要求(原文第18课公式未明确 3 走势是否含进入/离开走势)。

**整体定位**:段内笔中枢,**3 笔区间模型**(近 Vespa314 `over_seg`),不做级别升级、不做买卖点确认、不单独建模悬空进入/离开笔。

---

## 2. 数据结构

复用现有 `ZhongShu` 模型(`app/models/chan_model.py`),字段语义:

| 字段 | 类型 | 语义 |
|---|---|---|
| `start_index` | int | 中枢起始**缠论K线索引**(= 起始笔 start_index,与笔同语义) |
| `end_index` | int | 中枢结束**缠论K线索引**(= 结束笔 end_index) |
| `high` | float | ZG = min(参与笔高点) |
| `low` | float | ZD = max(参与笔低点) |
| `level` | int | 固定 1(笔中枢 = 1 级) |

> 不新增 GG/DD 字段(MVP 不绘制、不做级别升级,无需)。
> 笔高低点复用 `pen_high(pen, klines)` / `pen_low(pen, klines)`(`chan_helpers.py`)。

---

## 3. 算法规则(段内笔中枢)

### 3.1 主流程

```
输入:pens(笔列表)、klines(缠论K线)、segments(段列表)
1. 若 pens < 3 或 segments 为空 → 全局回退扫描(见 3.5)
2. 对每一段 seg(seg.start_index / seg.end_index 是笔索引,seg.direction 是段方向):
     在该段内的笔子序列 pens[seg.start_index .. seg.end_index] 上
     执行 3.2~3.4 的成中枢→延伸→破坏扫描(需传入段方向以识别反向笔,见 3.2)
3. 汇总所有段内产出的中枢
```

段由 `generate_segments(pens, klines)` 产出,段边界 = 笔索引边界,段与段首尾衔接。

### 3.2 成中枢【原文公式 + 缠论图示方向约定】

**方向约束(缠论图示约定)**:中枢以「反向笔」起止。
- 反向笔 = 与段方向相反的笔(向上段里是向下笔 X,向下段里是向上笔 S)
- 向上段中枢三笔形如 `X S X`(下-上-下);向下段形如 `S X S`(上-下-上)

扫描:从段内**第一根反向笔**开始,取连续 3 笔 `pen[i], pen[i+1], pen[i+2]`,其中 `pen[i]` 必须是反向笔:

```
若 pen[i] 不是反向笔  →  i += 1,找下一根反向笔
取三笔 p1=pen[i], p2=pen[i+1], p3=pen[i+2]
ZG = min(pen_high(p1), pen_high(p2), pen_high(p3))
ZD = max(pen_low(p1),  pen_low(p2),  pen_low(p3))
若 ZG > ZD  →  成中枢,区间 [ZD, ZG]
   起始 = p1.start_index(反向笔起点),结束 = p3.end_index(反向笔终点),进入延伸(3.3)
否则  →  从下一根反向笔重新开始(i += 1,不是逐笔滑动)
```

- `p1`、`p3` 同为反向笔(三笔方向为 反-同-反),故中枢以反向笔**起止**(成中枢的三笔,共 3 笔,满足「至少三笔」)。
- 对应原文第18课 `(max(a2,b2,c2), min(a1,b1,c1))`。
- 「不成中枢从下一反向笔重取」是缠论图示的严格约束,避免从同向笔起中枢。

### 3.3 延伸【结构约束:成对吸收】

中枢结构为 `X + (S X)*`(反向笔起,成对延伸),延伸以「同向笔+反向笔」**成对**吸收:

```
k 指向同向笔(相对成中枢起点为奇数偏移),k+1 指向其后的反向笔:
  若同向笔 pk 与区间不相交  →  pk 是离开笔,进入破坏判定(3.4)
  若同向笔 pk 相交:
    若 k+1 超出段尾  →  段尾只剩同向笔,无反向伙伴配对 → 保守结束(不纳入)
    若反向笔 pk1 也相交  →  成对延伸,end 落在 pk1(反向笔),k += 2
    若反向笔 pk1 不相交/离开  →  该对不能延伸,且同向笔不能作 end → 中枢在 pk 之前结束
```

- **结构不变量**:中枢笔数恒为**奇数(≥3)**,以反向笔**起止**(X + (S X)* 的对称结构)。
- 同向笔**永远不能**成为中枢的 end——它要么与反向笔成对延伸,要么是离开笔。
- 不设 9 段上限(0.7 后人演绎,MVP 不做级别升级)。
- 相交 = 笔全程价格区间与 [ZD,ZG] 有交集(全程极值,见 §3.6)。

### 3.4 破坏【原文:离开+回抽确认】

```
遇到不相交的笔 pen[k](离开笔) → 不立即破坏,看下一笔 pk1 回抽是否回到 [ZD,ZG]:
  若 k+1 超出段尾  →  保守结束(end 停在离开前最后一笔)
  按离开方向判回抽:
    pen[k] 向上离开(low > ZG):回抽笔回区间 = pen_low(pk1) <= ZG
    pen[k] 向下离开(high < ZD):回抽笔回区间 = pen_high(pk1) >= ZD
  回抽进区间  →  离开笔+回抽笔均属延伸,都纳入(继续扫描)
  回抽不进区间  →  确认破坏
破坏时:end_index 停在离开前最后一笔(离开笔不属于中枢,符合缠论"离开段是独立走势")
从离开笔之后(仍在本段内)重新开始 3.2 找下一个中枢
```

- 这是缠论原文判定(第20课中枢重新定理/中枢中心定理):「次级别走势离开中枢后,随后的次级别走势回抽不回到 [ZD,ZG] 才破坏」。破坏需**两笔**确认:离开笔 + 回抽笔。
- **关键**:回抽确认分支只有在笔用**全程极值**(§3.6)时才可达——端点K线版下相邻笔共享端点,回抽笔端点必在区间外,分支逻辑不可达,退化为"一笔离开即破坏"(已用 600000 次严格交替笔序列证明两版输出一致)。改用全程极值后,回抽笔的中间K线可回到区间,分支才真正激活。
- **与 Vespa314/tomcat123a 对齐**:两实现均为「1 笔离开即检查回抽」(Vespa314 `try_add_to_end` 失败即转 free_list,tomcat123a `grow` 设 `finished=1`),与本项目一致。
- **关于「两笔离开+第三笔拉回=延伸」(2026-07-19 研究记录)**:曾怀疑缠论原文有此更宽松规则(2 笔悬空+1 拉回算延伸),已构造用例证明现状会破坏该场景。但 Vespa314/tomcat123a 两实现均不支持 2-离开容忍,原文措辞无法联网核实。推测差异源于「走势≠笔」粒度(原文「次级别走势类型」是递归概念,可能跨多笔;Vespa314 用原子笔)。**决定:保持 1-离开+1-回抽确认**(与实现对齐)。若日后找到原文课程编号/截图证实 2-离开规则,再议。

### 3.5 全局回退【工程取舍】

当段未产出(笔不足3 或段算法无结果),退化为对全部笔做 3.2~3.4 扫描,避免中枢完全消失。

> 注:此回退路径下中枢可能跨段,但仅在段算法失效时触发,属降级方案。

### 3.6 笔高低点取全程极值【工程取舍·关键】

中枢扫描中,笔的 high/low 用 `_pen_range_high/_pen_range_low`(`chan_algorithm.py`):
取笔跨越的**全部缠论K线**的极值(含中间K线),而非仅端点K线(`chan_helpers.pen_high/pen_low`)。

- **为什么必须全程极值**:相邻笔共享端点(笔 i 的 end = 笔 i+1 的 start)。若用端点K线,离开笔的端点已在区间外时,回抽笔的端点(= 离开笔端点)也必在区间外,导致 §3.4 的回抽确认分支**逻辑上不可达**,破坏判定退化为"一笔离开即破坏"(已用 600000 次严格交替笔序列证明两版输出完全一致)。改用全程极值后,回抽笔的中间K线可回到区间,分支才真正激活,破坏判定贴近原文。
- **作用域**:仅中枢扫描用全程极值;段算法/笔校验仍用端点版 `pen_high/pen_low`,语义不变(段的 top/bottom 是段内最高笔的端点极值,符合线段定义)。
- 单元验证(全程极值版):成中枢/延伸/回抽进区间延伸/回抽不进区间破坏(end 停离开前)/段尾保守结束/ZG-ZD 不变性,全过;且构造出端点版不可能的"回抽笔中间K线回区间→延伸"用例。

---

## 4. 链路透传(实现时必做)

| 层 | 现状 | 需做 |
|---|---|---|
| 算法 `identify_zhongshus` | 现为滑窗雏形(无延伸/破坏) | 按 §3 重写 |
| API `chan.py` `data.chan` | **缺 zhongshus 字段** | 补 `"zhongshus": [z.model_dump() ...]` |
| 前端 `KlineChart.vue` | 用 `klineData[zs.start_index]` 取日期(**错**,那是缠论K线索引) | 经 `chan_klines[zs.start_index].start`/`chan_klines[zs.end_index].end` 映射回原始K线索引区间 |

> 中枢绘制(双 dashed line 上下沿)代码框架已存在,仅需修正索引映射。

---

## 5. 验证

1. **类型检查**:`conda run -n ws mypy .`(中枢代码零新增错误;`process_inclusion:31` 的历史错误与本功能无关)
2. **算法单元验证**(纯逻辑,不依赖 MySQL/Redis):构造笔序列,验证
   - 段内成中枢区间 ZG/ZD 计算正确
   - 相交延伸正确推进 end_index
   - 离开笔触发破坏、段内重启找下一个
   - 段为空时全局回退不丢中枢
   - **跨段违规数 = 0**(每个中枢完整落在某段内)
3. **地基验证(关键)**:拉一只真实股票,打印笔+段,肉眼对 K 线图确认段算法可信——段是地基,地基不对中枢必错。
4. **端到端**:起 MySQL+Redis+前后端,选波动丰富股票,勾「中枢」:
   - 上下沿对齐笔的高低点
   - 横跨日期正确(经 chan_klines 映射后)
   - 数量统计合理
   - 切周期/主题正确重渲染
   - <3 笔时中枢为空不报错

---

## 6. 不在本期范围(明确排除)

- 段中枢(用段构造)、中枢级别升级(9段/三中枢重叠)、第三类买卖点、走势递归
- GG/DD 区间可视化
- 9 段上限(后人演绎,非原文)
- CLAUDE.md 关于"段和中枢是 dead code"的过时描述(留待统一整理)

---

## 7. 后续演进路径(参考)

若 MVP 验证后想升级,按证据强度排序的演进方向:

1. **放开跨段**(`zs_algo=over_seg`):若发现不跨段导致中枢偏少——纯工程切换,无理论成本
2. **加第三类买卖点确认破坏**(0.7 原文间接):让破坏判定更贴近原文
3. **段中枢**(三段重叠):高级别中枢,tomcat123a 路线
4. **级别升级**:9段/三中枢重叠——注意 9 段是后人演绎,需自行决定是否采纳
