# 代码规范

## 环境与依赖管理
- **包管理器**：强制使用 conda 进行依赖和环境管理。
- **虚拟环境**：本项目的专属环境名为 `ws`。
- **命令执行**：所有 Python 相关的脚本、测试和检查命令，必须通过 `conda run -n ws <command>` 或在激活 `ws` 环境后执行。

## 类型检查 (mypy)
- **工具要求**：强制使用 mypy 进行静态类型检查。
- **运行命令**：修改代码后，请主动运行 `conda run -n ws mypy .` （或指定具体目录）进行检查。
- **报错处理原则**：
  - 优先通过补全或修正类型声明来解决报错。
  - 严禁滥用 `Any` 或使用 `# type: ignore` 来掩盖错误。
  - 如果确实无法解决，需向我说明原因后再添加忽略注释。