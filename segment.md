# 缠论段划分算法

## 1. 基本概念

### 1.1 笔的表示

- **S** = 向上笔（up）
- **X** = 向下笔（down）

线段只有两种起点：
- 从向上笔开始：S₁X₁S₂X₂S₃X₃…SₙXₙ
- 从向下笔开始：X₁S₁X₂S₂X₃S₃…XₙSₙ

本文以**向上笔开始的线段**为例推导规则，向下笔开始的线段对称处理。

### 1.2 关键性质

> 任何 Sᵢ 与 Sᵢ₊₁ 之间，一定有重合区间（因为笔必须交替，相邻向上笔之间必有向下笔连接，且构成笔的条件保证了重叠）。

> 而 Xᵢ 与 Xᵢ₊₁ 之间，**不一定**有重合区间。因此 X 序列更能代表线段的性质。

---

## 2. 特征序列

### 2.1 定义

| 线段类型 | 笔序列 | 特征序列 |
|---------|--------|---------|
| 向上笔开始 | S₁X₁S₂X₂…SₙXₙ | X₁, X₂, …, Xₙ（所有向下笔） |
| 向下笔开始 | X₁S₁X₂S₂…XₙSₙ | S₁, S₂, …, Sₙ（所有向上笔） |

**本质**：特征序列 = 与线段方向相反的笔的序列。

### 2.2 特征序列的缺口

特征序列两相邻元素之间没有重合区间，称为**缺口**。

两个笔 Xᵢ 与 Xᵢ₊₁ 有重合（无缺口）：
```
Xᵢ.high ≥ Xᵢ₊₁.low  且  Xᵢ₊₁.high ≥ Xᵢ.low
```

没有重合（有缺口）：
```
Xᵢ.low > Xᵢ₊₁.high   （Xᵢ 完全在 Xᵢ₊₁ 上方）
或
Xᵢ₊₁.low > Xᵢ.high   （Xᵢ₊₁ 完全在 Xᵢ 上方）
```

---

## 3. 标准特征序列

### 3.1 包含关系处理

将特征序列中的每个元素视为一根"K线"（high = 笔的最高价，low = 笔的最低价），对相邻元素进行**包含关系处理**，方法与K线包含处理完全一致：

- 上升趋势：取 high 中较高的 high，取 low 中较高的 low
- 下降趋势：取 high 中较低的 high，取 low 中较低的 low

处理后的序列称为**标准特征序列**。

> 后续文中"特征序列"如无特别说明，均指标准特征序列。

---

## 4. 特征序列的分型

### 4.1 分型识别

将标准特征序列的每个元素视为K线，按照与K线分型相同的方法识别顶分型和底分型：

- **顶分型**：中间元素的 high 是三者最高，low 也是三者最高
- **底分型**：中间元素的 low 是三者最低，high 也是三者最低

### 4.2 方向过滤

| 线段类型 | 只考察 |
|---------|-------|
| 向上笔开始的线段 | **顶分型** |
| 向下笔开始的线段 | **底分型** |

> 向上段只看顶分型——因为向上段的结束需要看到"转折向下"的信号，顶分型就是特征序列中由升转降的标志。

---

## 5. 线段结束的判定（核心规则）

线段结束需同时满足两个条件，取两者中更早的断点：

1. **特征序列分型条件**（§5.1/5.2）
2. **同向笔重合条件**（§5.3）

### 5.1 第一种情况：无缺口

> 特征序列的分型中，**第一元素和第二元素之间不存在缺口**（即有重合区间）。

**判定**：线段在该分型的极值点处结束。

**注意**：经过包含处理后，标准特征序列的一个元素可能由多个原始笔合并而来。分型的第二元素可能包含多根笔，此时应在这些笔中找到产生极值的那根笔，在该笔处划分线段。

| 线段类型 | 分型类型 | 极值类型 | 结束位置 |
|---------|---------|---------|---------|
| 向上笔开始 | 顶分型 | 最高点 | 第二元素的 pen_indices 中 high 最高的笔 |
| 向下笔开始 | 底分型 | 最低点 | 第二元素的 pen_indices 中 low 最低的笔 |

```
特征序列（向上段的向下笔）：
         X₂  ← 第二元素（分型顶）
        / \
       /   \
      X₁    X₃  ← 第一、三元素
      ↑
  X₁与X₂有重合（无缺口）

→ 向上段在 X₂ 的高点处结束
```

### 5.2 第二种情况：有缺口

> 特征序列的分型中，**第一元素和第二元素之间存在缺口**（即无重合区间）。

此时不能直接判定线段结束，需要**进一步验证**：

| 线段类型 | 分型类型 | 验证方式 |
|---------|---------|---------|
| 向上笔开始 | 顶分型（有缺口） | 从该顶分型**极值笔**（pen_indices 中 high 最高的笔）开始的向下笔，构造**新线段**的特征序列，若出现**底分型** → 向上段结束 |
| 向下笔开始 | 底分型（有缺口） | 从该底分型**极值笔**（pen_indices 中 low 最低的笔）开始的向上笔，构造**新线段**的特征序列，若出现**顶分型** → 向下段结束 |

**关键要点**：

1. 第二种情况下的验证序列中出现的分型，**不区分第一/二种情况**，只要有分型即可。
2. 后一特征序列**不一定**封闭前一特征序列相应的缺口。
3. 验证的是"新线段"的特征序列——即从分型极值点开始，笔的方向已反转，要构造的是反向线段的特征序列。

```
特征序列（向上段的向下笔）：
      X₂  ← 第二元素（分型顶）
     /    \
    /      X₃
   /
  X₁  ← 第一元素
  ↑
  X₁与X₂无重合（有缺口）

→ 需要验证：从 X₂ 高点开始的向下笔序列
→ 构造新线段（向下段）的特征序列（即 S 序列）
→ 若该 S 序列出现底分型 → 原向上段结束
→ 若不出现底分型 → 原向上段继续
```

### 5.3 同向笔重合条件

> 向上段 S₁X₁S₂X₂…SₙXₙ 中，任何 Sᵢ 与 Sᵢ₊₁ 之间**必须**有重合区间。若 Sᵢ 与 Sᵢ₊₁ 无重合，则段终结于 Sᵢ。
> 向下段 X₁S₁X₂S₂…XₙSₙ 中，任何 Xᵢ 与 Xᵢ₊₁ 之间**必须**有重合区间。若 Xᵢ 与 Xᵢ₊₁ 无重合，则段终结于 Xᵢ。

**重合判断**：两根同向笔的价格区间有交集即可：
```
min(pen_a.high, pen_b.high) >= max(pen_a.low, pen_b.low)
```

**与其他条件的关系**：同向笔重合条件与特征序列分型条件**独立**判定，取两者中更早的断点作为段的结束位置。

```
向上段 S₁X₁S₂X₂S₃X₃：
  S₁ = [10, 20]  S₂ = [25, 35]  → S₁.high(20) < S₂.low(25) → 无重合！
  → 段终结于 S₁，S₂ 开始新段
```

---

## 6. 完整算法流程

### 输入

- 笔列表 `pens`（已校验，方向严格交替 up/down/up/down/...）
- 缠论K线列表 `klines`（用于获取笔的高低点）

### 步骤

```
1. 初始化 seg_start = 0，seg_direction = pens[0].direction
2. 检查同向笔重合约束：
   a. 收集段内所有同向笔，逐对检查是否有重合区间
   b. 若 Sᵢ 与 Sᵢ₊₁ 无重合 → 段在 Sᵢ 处结束（overlap_break_idx）
3. 收集当前段的特征序列元素（从 seg_start 开始，取与段方向相反的笔）
4. 对特征序列进行包含关系处理 → 标准特征序列（先验方向=段方向）
5. 在标准特征序列中识别分型（向上段找顶分型，向下段找底分型）
6. 若找到分型：
   a. 在第二元素的 pen_indices 中找极值笔
   b. 判断第一元素和第二元素之间是否有缺口
   c. 无缺口（第一种情况）→ 段在极值笔处结束（char_end_idx）
   d. 有缺口（第二种情况）→ 构造验证序列：
      - 从极值笔开始，构造反向段的特征序列
      - 对该序列做包含处理和分型识别
      - 若出现分型（任何类型）→ 段在极值笔处结束（char_end_idx）
      - 若未出现分型 → 当前段继续，尝试下一个分型
7. 取 overlap_break_idx 和 char_end_idx 中更早的断点作为段结束
8. 段结束后，新段从下一根笔开始，方向反转，回到步骤 2
9. 遍历完所有笔后，处理尾部段（剩余笔数 ≥ 3 且为奇数则独立成段）
10. 后处理校验（validate_segments）：
    - 每段笔数 ≥ 3 且为奇数
    - 段方向 = 起始笔方向 = 终止笔方向
    - 相邻段方向交替
```

---

## 7. 与 v1 算法的对比

| 维度 | v1 算法 | 新算法（特征序列法） |
|------|---------|------------------|
| 断段依据 | 同向笔价格比较（高低点都更低/更高） | 特征序列的分型 + 同向笔重合约束 |
| 缺口处理 | 无 | 区分有缺口/无缺口两种情况，有缺口需验证 |
| 包含处理 | 无 | 对特征序列做包含处理，生成标准特征序列 |
| 同向笔重合 | 无 | Si 与 Si+1 无重合则段终结 |
| 断段位置 | `prev_same_dir_idx - 1`（同向笔的前一笔） | 分型的极值笔（经包含处理后在 pen_indices 中查找） |
| 理论基础 | 简化规则 | 缠论原文定义，更精确 |
